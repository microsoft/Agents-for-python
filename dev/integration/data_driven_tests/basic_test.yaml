parent: ./defaults.yaml
description: A basic data driven test example
defaults:
  input:
  sleep:
  assertion:
test:
  - type: input
    activity:
      type: message
      text: "Hello, World!"

  - type: sleep
    duration: 1

  - type: assertion
    quantifier: ONE
    selector:
      index: -1
      quantifier: ONE
      activity:
        type: message
    activity:
      type: message
      text: "Hello, World!"