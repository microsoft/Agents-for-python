parent: ./defaults.yaml
description: A basic data driven test example
defaults:
  input:
  sleep:
  assertion:
test:
  - type: input
    activity:
      type: message
      text: "Hello, World!"

  - type: sleep
    duration: 1

  - type: assertion
    selector:
      index: -1
      quantifier: ONE # implied
      activity:
        type: message
    activity:
      type: message
      text: "Hello, World!"